# This file is intentionally minimal. The Codespace devcontainer uses a single-container devcontainer.json
# which runs the official NodeJS devcontainer image. MongoDB is started from inside the devcontainer via
# the postStartCommand using the top-level docker-compose.yml in the repository.
# Keeping this file here avoids interfering with the devcontainer CLI's compose merge steps.
