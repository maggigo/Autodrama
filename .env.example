# Example environment variables for running Autodrama locally or in Codespaces.
# Copy this to a file named ".env" in the repository root if you want to run locally.

MONGODB_URI=mongodb://localhost:27017/autodrama
JWT_SECRET=change_this_to_a_random_string
